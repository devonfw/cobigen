package ${variables.rootPackage}.general.common.base;

/**
 * This is the abstract base class for every implementation class (every layer). The class provides the functionality to
 * map different layer entities.
 * 
 * @author hohwille
 */
public abstract class AbstractLayerImpl extends AbstractBeanMapperSupport {

}
